import { createIcon } from 'create-icon'

const icons = ['camera', 'camera-active', 'chat', 'chat-active', 'task', 'task-active', 'user', 'user-active']

icons.forEach(name => {
  createIcon({
    name,
    output: './assets/icons'
  })
})
