# 数据库初始化指南

## 前置要求

- MySQL 5.7 或更高版本
- 已安装 MySQL 客户端工具
- 拥有数据库创建权限的 MySQL 用户

## 快速开始

### 1. 确认数据库脚本位置

数据库初始化脚本位于：
```
backend/config/init.sql
```

### 2. 执行初始化脚本

#### 方式一：命令行执行（推荐）

```bash
# 进入项目根目录
cd /path/to/AttendanceAgent

# 执行初始化脚本（需要输入 MySQL root 密码）
mysql -u root -p < backend/config/init.sql
```

#### 方式二：MySQL 客户端内执行

```sql
-- 1. 登录 MySQL
mysql -u root -p

-- 2. 执行脚本
source /path/to/AttendanceAgent/backend/config/init.sql;
```

#### 方式三：使用数据库管理工具

如果你使用 Navicat、DBeaver、phpMyAdmin 等工具：

1. 打开工具并连接到 MySQL 服务器
2. 新建查询窗口
3. 打开 `backend/config/init.sql` 文件
4. 复制全部内容并执行

## 数据库结构说明

初始化脚本会创建以下表：

### 1. users（用户表）
存储系统用户信息，包括：
- 用户基本信息（用户名、邮箱、密码）
- 飞书用户关联
- 角色权限（admin/user）
- 登录状态记录

### 2. tasks（任务表）
存储考勤识别任务信息：
- 任务状态（processing/processed/confirmed/failed/cancelled）
- AI 解析原始数据
- 用户确认数据
- 处理记录

### 3. plugin_config（配置表）
存储系统配置参数：
- 飞书多维表格配置
- 字段映射配置
- 系统参数配置

### 4. audit_logs（审计日志表）
记录用户操作审计日志

### 5. logs（日志表）
记录系统运行日志

## 默认账号

初始化后会创建一个默认管理员账号：

| 项目 | 值 |
|------|-----|
| 用户名 | admin |
| 邮箱 | admin@example.com |
| 密码 | admin123 |
| 角色 | admin |

⚠️ **重要提示**：首次登录后请立即修改默认密码！

## 验证安装

执行完初始化脚本后，验证数据库是否创建成功：

```sql
-- 登录 MySQL
mysql -u root -p

-- 查看数据库
SHOW DATABASES;

-- 应该能看到 attendance_assistant 数据库

-- 使用数据库
USE attendance_assistant;

-- 查看表
SHOW TABLES;

-- 应该能看到 5 个表

-- 查看管理员用户
SELECT id, username, email, role FROM users;
```

## 常见问题

### Q: 提示 "Access denied for user 'root'@'localhost'"
A: 请检查你的 MySQL root 密码是否正确，或使用有创建数据库权限的其他用户。

### Q: 数据库已存在怎么办？
A: 脚本使用了 `CREATE DATABASE IF NOT EXISTS` 和 `CREATE TABLE IF NOT EXISTS`，可以安全地重复执行，不会覆盖已有数据。

### Q: 如何清空数据重新初始化？
A: 
```sql
-- 删除数据库
DROP DATABASE IF EXISTS attendance_assistant;

-- 重新执行初始化脚本
```

### Q: 如何修改数据库名称？
A: 编辑 `backend/config/init.sql` 文件，将第 2 行的 `attendance_assistant` 修改为你想要的名称，同时需要同步修改后端配置文件中的数据库连接配置。

## 数据库连接配置

后端数据库连接配置文件：
- `backend/src/main/resources/application-dev.yml`（开发环境）
- `backend/src/main/resources/application-prod.yml`（生产环境）

配置示例：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance_assistant?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
```

## 备份与恢复

### 备份数据库
```bash
mysqldump -u root -p attendance_assistant > backup.sql
```

### 恢复数据库
```bash
mysql -u root -p attendance_assistant < backup.sql
```

## 下一步

数据库初始化完成后：
1. 配置后端数据库连接
2. 启动后端服务
3. 使用默认账号登录系统
4. 修改默认密码
5. 配置飞书应用信息
