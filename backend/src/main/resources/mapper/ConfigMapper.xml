<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.ConfigMapper">

    <resultMap id="BaseResultMap" type="com.attendance.entity.PluginConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, config_key, config_value, config_type, description, created_at, updated_at
    </sql>

    <select id="selectConfigByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin_config
        WHERE config_key = #{configKey}
    </select>

    <select id="selectAllConfigs" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM plugin_config
        ORDER BY config_key
    </select>

    <insert id="insertConfig" parameterType="com.attendance.entity.PluginConfig">
        INSERT INTO plugin_config (config_key, config_value, config_type, description, created_at, updated_at)
        VALUES (#{configKey}, #{configValue}, #{configType}, #{description}, NOW(), NOW())
    </insert>

    <update id="updateConfigByKey">
        UPDATE plugin_config
        SET config_value = #{configValue}, updated_at = NOW()
        WHERE config_key = #{configKey}
    </update>

    <delete id="deleteConfigByKey">
        DELETE FROM plugin_config WHERE config_key = #{configKey}
    </delete>

    <select id="existsByKey" resultType="int">
        SELECT COUNT(*) FROM plugin_config WHERE config_key = #{configKey}
    </select>

</mapper>
