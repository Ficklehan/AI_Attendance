<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="com.attendance.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="action" property="action"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="details" property="details"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, username, action, target_type, target_id, details, created_at
    </sql>

    <select id="selectAuditLogList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM audit_logs
        WHERE 1=1
        <if test="userId != null and userId != ''">
            AND user_id = #{userId}
        </if>
        <if test="action != null and action != ''">
            AND action = #{action}
        </if>
        <if test="startDate != null and startDate != ''">
            AND created_at >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND created_at &lt;= #{endDate}
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countAuditLogList" resultType="long">
        SELECT COUNT(*)
        FROM audit_logs
        WHERE 1=1
        <if test="userId != null and userId != ''">
            AND user_id = #{userId}
        </if>
        <if test="action != null and action != ''">
            AND action = #{action}
        </if>
        <if test="startDate != null and startDate != ''">
            AND created_at >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND created_at &lt;= #{endDate}
        </if>
    </select>

    <insert id="insertAuditLog" parameterType="com.attendance.entity.AuditLog">
        INSERT INTO audit_logs (user_id, username, action, target_type, target_id, details, created_at)
        VALUES (#{userId}, #{username}, #{action}, #{targetType}, #{targetId}, #{details}, NOW())
    </insert>

</mapper>
