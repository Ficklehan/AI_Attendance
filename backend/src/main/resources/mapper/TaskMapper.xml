<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.TaskMapper">

    <resultMap id="BaseResultMap" type="com.attendance.entity.Task">
        <id column="task_id" property="taskId"/>
        <result column="user_id" property="userId"/>
        <result column="file_key" property="fileKey"/>
        <result column="status" property="status"/>
        <result column="sync_status" property="syncStatus"/>
        <result column="sync_error" property="syncError"/>
        <result column="prompt_country" property="promptCountry"/>
        <result column="raw_data" property="rawData"/>
        <result column="confirmed_data" property="confirmedData"/>
        <result column="image_urls" property="imageUrls"/>
        <result column="anomaly_summary" property="anomalySummary"/>
        <result column="ai_raw_output" property="aiRawOutput"/>
        <result column="processed_by" property="processedBy"/>
        <result column="progress_row_count" property="progressRowCount"/>
        <result column="recognition_checkpoint" property="recognitionCheckpoint"/>
        <result column="recognition_heartbeat_at" property="recognitionHeartbeatAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        task_id, user_id, file_key, status, sync_status, sync_error, prompt_country,
        raw_data, confirmed_data, image_urls, anomaly_summary, ai_raw_output, processed_by,
        progress_row_count, recognition_checkpoint, recognition_heartbeat_at,
        created_at, updated_at
    </sql>

    <sql id="DataScopeTaskWhere">
        <if test="scope != null and !scope.allUsers">
            <choose>
                <when test="(scope.ownerUserIds == null or scope.ownerUserIds.size() == 0) and !scope.recordDimensionFilter">
                    AND 1=0
                </when>
                <otherwise>
                    <if test="scope.ownerUserIds != null and scope.ownerUserIds.size() > 0">
                        AND t.user_id IN
                        <foreach collection="scope.ownerUserIds" item="uid" open="(" separator="," close=")">
                            #{uid}
                        </foreach>
                    </if>
                    <if test="scope.recordDimensionFilter">
                        AND (
                            EXISTS (
                                SELECT 1 FROM task_records tr_ds
                                WHERE tr_ds.task_id = t.task_id AND tr_ds.deleted = 0
                                <if test="scope.countries != null and scope.countries.size() > 0">
                                    AND tr_ds.country_key IN
                                    <foreach collection="scope.countries" item="cv" open="(" separator="," close=")">#{cv}</foreach>
                                </if>
                                <if test="scope.warehouses != null and scope.warehouses.size() > 0">
                                    AND tr_ds.warehouse_key IN
                                    <foreach collection="scope.warehouses" item="wv" open="(" separator="," close=")">#{wv}</foreach>
                                </if>
                                <if test="scope.agencies != null and scope.agencies.size() > 0">
                                    AND tr_ds.agency_key IN
                                    <foreach collection="scope.agencies" item="av" open="(" separator="," close=")">#{av}</foreach>
                                </if>
                            )
                            <if test="scope.viewerUserId != null and scope.viewerUserId != ''">
                                OR t.user_id = #{scope.viewerUserId}
                            </if>
                        )
                    </if>
                </otherwise>
            </choose>
        </if>
    </sql>

    <select id="selectTaskByTaskId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tasks
        WHERE task_id = #{taskId}
    </select>

    <select id="selectTaskProgress" resultType="com.attendance.dto.response.TaskProgressDTO">
        SELECT task_id AS taskId,
               status,
               sync_status AS syncStatus,
               sync_error AS syncError,
               ai_raw_output AS aiRawOutput,
               COALESCE(progress_row_count, 0) AS progressRowCount,
               anomaly_summary AS anomalySummaryRaw
        FROM tasks
        WHERE task_id = #{taskId}
    </select>

    <resultMap id="TaskListRowMap" type="com.attendance.entity.TaskListRow">
        <id column="task_id" property="taskId"/>
        <result column="user_id" property="userId"/>
        <result column="file_key" property="fileKey"/>
        <result column="status" property="status"/>
        <result column="sync_status" property="syncStatus"/>
        <result column="sync_error" property="syncError"/>
        <result column="image_urls" property="imageUrls"/>
        <result column="progress_row_count" property="progressRowCount"/>
        <result column="created_at" property="createdAt"/>
        <result column="user_name" property="userName"/>
    </resultMap>

    <select id="selectTaskList" resultMap="TaskListRowMap">
        SELECT t.task_id, t.user_id, t.file_key, t.status, t.sync_status, t.sync_error,
               t.image_urls, COALESCE(t.progress_row_count, 0) AS progress_row_count, t.created_at,
               COALESCE(u.real_name, u.username, '') AS user_name
        FROM tasks t
        LEFT JOIN users u ON t.user_id = u.id
        WHERE 1=1
        <include refid="DataScopeTaskWhere"/>
        <if test="status != null and status != ''">
            AND t.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="keywordField == 'taskId'">
                    AND t.task_id LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="keywordField == 'fileKey'">
                    AND t.file_key LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="keywordField == 'userName'">
                    AND (u.real_name LIKE CONCAT('%', #{keyword}, '%') OR u.username LIKE CONCAT('%', #{keyword}, '%'))
                </when>
                <when test="keywordField == 'status'">
                    AND t.status = #{keyword}
                </when>
                <when test="keywordField == 'createdAt'">
                    <choose>
                        <when test="keyword.indexOf('|') &gt; -1">
                            <bind name="dtRange" value="keyword.split('\\|')"/>
                            <if test="dtRange.length &gt; 0 and dtRange[0] != null and dtRange[0] != ''">
                                AND t.created_at &gt;= STR_TO_DATE(#{dtRange[0]}, '%Y-%m-%d %H:%i:%s')
                            </if>
                            <if test="dtRange.length &gt; 1 and dtRange[1] != null and dtRange[1] != ''">
                                AND t.created_at &lt;= STR_TO_DATE(#{dtRange[1]}, '%Y-%m-%d %H:%i:%s')
                            </if>
                        </when>
                        <otherwise>
                            AND DATE_FORMAT(t.created_at, '%Y-%m-%d %H:%i:%s') LIKE CONCAT('%', #{keyword}, '%')
                        </otherwise>
                    </choose>
                </when>
                <otherwise>
                    AND (t.task_id LIKE CONCAT('%', #{keyword}, '%') OR t.file_key LIKE CONCAT('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </if>
        ORDER BY t.created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countTaskList" resultType="long">
        SELECT COUNT(*)
        FROM tasks t
        LEFT JOIN users u ON t.user_id = u.id
        WHERE 1=1
        <include refid="DataScopeTaskWhere"/>
        <if test="status != null and status != ''">
            AND t.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="keywordField == 'taskId'">
                    AND t.task_id LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="keywordField == 'fileKey'">
                    AND t.file_key LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="keywordField == 'userName'">
                    AND (u.real_name LIKE CONCAT('%', #{keyword}, '%') OR u.username LIKE CONCAT('%', #{keyword}, '%'))
                </when>
                <when test="keywordField == 'status'">
                    AND t.status = #{keyword}
                </when>
                <when test="keywordField == 'createdAt'">
                    <choose>
                        <when test="keyword.indexOf('|') &gt; -1">
                            <bind name="dtRange" value="keyword.split('\\|')"/>
                            <if test="dtRange.length &gt; 0 and dtRange[0] != null and dtRange[0] != ''">
                                AND t.created_at &gt;= STR_TO_DATE(#{dtRange[0]}, '%Y-%m-%d %H:%i:%s')
                            </if>
                            <if test="dtRange.length &gt; 1 and dtRange[1] != null and dtRange[1] != ''">
                                AND t.created_at &lt;= STR_TO_DATE(#{dtRange[1]}, '%Y-%m-%d %H:%i:%s')
                            </if>
                        </when>
                        <otherwise>
                            AND DATE_FORMAT(t.created_at, '%Y-%m-%d %H:%i:%s') LIKE CONCAT('%', #{keyword}, '%')
                        </otherwise>
                    </choose>
                </when>
                <otherwise>
                    AND (t.task_id LIKE CONCAT('%', #{keyword}, '%') OR t.file_key LIKE CONCAT('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </if>
    </select>

    <select id="selectLastTaskId" resultType="string">
        SELECT task_id FROM tasks ORDER BY created_at DESC LIMIT 1
    </select>

    <select id="selectTaskByFileKeyForScope" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tasks t
        WHERE (t.file_key = #{fileKey} OR t.image_urls LIKE CONCAT('%', #{fileKey}, '%'))
        <include refid="DataScopeTaskWhere"/>
        ORDER BY t.created_at DESC
        LIMIT 1
    </select>

    <select id="selectTaskByFileKeyAndUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tasks t
        WHERE t.user_id = #{userId}
          AND (t.file_key = #{fileKey} OR t.image_urls LIKE CONCAT('%', #{fileKey}, '%'))
        ORDER BY t.created_at DESC
        LIMIT 1
    </select>

    <insert id="insertTask" parameterType="com.attendance.entity.Task">
        INSERT INTO tasks (task_id, user_id, file_key, status, sync_status, prompt_country,
                          raw_data, confirmed_data, image_urls, anomaly_summary, ai_raw_output,
                          processed_by, created_at, updated_at)
        VALUES (#{taskId}, #{userId}, #{fileKey}, #{status}, #{syncStatus}, #{promptCountry},
                #{rawData}, #{confirmedData}, #{imageUrls}, #{anomalySummary}, #{aiRawOutput},
                #{processedBy}, NOW(), NOW())
    </insert>

    <update id="updateTask" parameterType="com.attendance.entity.Task">
        UPDATE tasks
        SET status = #{status},
            raw_data = #{rawData},
            confirmed_data = #{confirmedData},
            image_urls = #{imageUrls},
            anomaly_summary = #{anomalySummary},
            ai_raw_output = #{aiRawOutput},
            processed_by = #{processedBy},
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskStatus">
        UPDATE tasks SET status = #{status}, updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskRawData">
        UPDATE tasks 
        SET raw_data = #{rawData},
            ai_raw_output = #{aiRawOutput},
            progress_row_count = #{progressRowCount},
            status = 'processed',
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskRawDataProgress">
        UPDATE tasks
        SET raw_data = #{rawData},
            ai_raw_output = #{aiRawOutput},
            progress_row_count = #{progressRowCount},
            updated_at = NOW()
        WHERE task_id = #{taskId} AND status = 'processing'
    </update>

    <update id="updateTaskRecognitionProgress">
        UPDATE tasks
        SET progress_row_count = #{progressRowCount},
            ai_raw_output = #{aiRawOutput},
            updated_at = NOW()
        WHERE task_id = #{taskId} AND status = 'processing'
    </update>

    <update id="updateTaskConfirmedData">
        UPDATE tasks 
        SET confirmed_data = #{confirmedData}, status = 'confirmed', updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskRecordPayload">
        UPDATE tasks
        SET raw_data = #{rawData},
            confirmed_data = #{confirmedData},
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskSyncStatus">
        UPDATE tasks
        SET sync_status = #{syncStatus},
            sync_error = #{syncError},
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskImageUrls">
        UPDATE tasks
        SET image_urls = #{imageUrls}, updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateTaskAnomalySummary">
        UPDATE tasks
        SET anomaly_summary = #{anomalySummary}, updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="updateRecognitionCheckpoint">
        UPDATE tasks
        SET recognition_checkpoint = #{checkpoint},
            recognition_heartbeat_at = NOW(),
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="clearRecognitionCheckpoint">
        UPDATE tasks
        SET recognition_checkpoint = NULL,
            updated_at = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="touchRecognitionHeartbeat">
        UPDATE tasks
        SET recognition_heartbeat_at = NOW(),
            updated_at = NOW()
        WHERE task_id = #{taskId} AND status = 'processing'
    </update>

    <select id="selectStaleProcessingTaskIds" resultType="string">
        SELECT task_id
        FROM tasks
        WHERE status = 'processing'
          AND (
            (recognition_heartbeat_at IS NOT NULL
             AND recognition_heartbeat_at &lt; DATE_SUB(NOW(), INTERVAL #{staleSeconds} SECOND))
            OR
            (recognition_heartbeat_at IS NULL
             AND updated_at &lt; DATE_SUB(NOW(), INTERVAL #{staleSeconds} SECOND))
          )
        ORDER BY updated_at ASC
        LIMIT #{batchSize}
    </select>

    <select id="selectZombieProcessingTaskIds" resultType="string">
        SELECT task_id
        FROM tasks
        WHERE status = 'processing'
          AND recognition_heartbeat_at IS NULL
          AND updated_at &lt; DATE_SUB(NOW(), INTERVAL #{zombieMinutes} MINUTE)
        ORDER BY updated_at ASC
        LIMIT #{batchSize}
    </select>

    <select id="selectTasksForDuplicateByStatuses" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tasks
        WHERE status IN
        <foreach item="s" collection="statuses" open="(" separator="," close=")">
            #{s}
        </foreach>
          AND confirmed_data IS NOT NULL
          AND confirmed_data != ''
          <if test="excludeTaskId != null and excludeTaskId != ''">
            AND task_id != #{excludeTaskId}
          </if>
        ORDER BY updated_at DESC
    </select>

    <select id="countTasksGroupByStatus" resultType="map">
        SELECT t.status AS status, COUNT(*) AS cnt
        FROM tasks t
        WHERE 1=1
        <include refid="DataScopeTaskWhere"/>
        GROUP BY t.status
    </select>

    <select id="selectTasksForRecordView" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tasks t
        WHERE 1=1
        <include refid="DataScopeTaskWhere"/>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <delete id="deleteTaskByTaskId">
        DELETE FROM tasks WHERE task_id = #{taskId}
    </delete>

</mapper>
