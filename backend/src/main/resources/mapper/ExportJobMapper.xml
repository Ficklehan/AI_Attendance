<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.ExportJobMapper">

    <resultMap id="BaseResultMap" type="com.attendance.entity.ExportJob">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="export_type" property="exportType"/>
        <result column="status" property="status"/>
        <result column="query_json" property="queryJson"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="row_count" property="rowCount"/>
        <result column="error_message" property="errorMessage"/>
        <result column="created_at" property="createdAt"/>
        <result column="completed_at" property="completedAt"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="dismissed_at" property="dismissedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, export_type, status, query_json, file_name, file_path,
        row_count, error_message, created_at, completed_at, expires_at, dismissed_at
    </sql>

    <sql id="ActiveScopeWhere">
        AND (
            status IN ('pending', 'running')
            OR (status IN ('completed', 'failed') AND dismissed_at IS NULL)
        )
    </sql>

    <insert id="insert" parameterType="com.attendance.entity.ExportJob">
        INSERT INTO export_jobs (
            id, user_id, export_type, status, query_json, file_name, file_path,
            row_count, error_message, created_at, expires_at
        ) VALUES (
            #{id}, #{userId}, #{exportType}, #{status}, #{queryJson}, #{fileName}, #{filePath},
            #{rowCount}, #{errorMessage}, NOW(), #{expiresAt}
        )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM export_jobs WHERE id = #{id}
    </select>

    <select id="selectByIdAndUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM export_jobs
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM export_jobs
        WHERE user_id = #{userId}
        <if test="scope == null or scope == '' or scope == 'active'">
            <include refid="ActiveScopeWhere"/>
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM export_jobs
        WHERE user_id = #{userId}
        <if test="scope == null or scope == '' or scope == 'active'">
            <include refid="ActiveScopeWhere"/>
        </if>
    </select>

    <select id="countActiveByUserId" resultType="long">
        SELECT COUNT(*)
        FROM export_jobs
        WHERE user_id = #{userId}
        <include refid="ActiveScopeWhere"/>
    </select>

    <update id="dismissFinishedByUserId">
        UPDATE export_jobs
        SET dismissed_at = NOW()
        WHERE user_id = #{userId}
          AND status IN ('completed', 'failed')
          AND dismissed_at IS NULL
    </update>

    <update id="updateStatus">
        UPDATE export_jobs
        SET status = #{status},
            error_message = #{errorMessage}
        WHERE id = #{id}
    </update>

    <update id="updateCompleted">
        UPDATE export_jobs
        SET status = #{status},
            file_name = #{fileName},
            file_path = #{filePath},
            row_count = #{rowCount},
            error_message = #{errorMessage},
            completed_at = NOW()
        WHERE id = #{id}
    </update>
</mapper>
