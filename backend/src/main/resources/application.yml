server:
  port: 8080
  servlet:
    context-path: /attendance/api

spring:
  profiles:
    active: dev
  application:
    name: attendance-assistant
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.attendance.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: ${JWT_SECRET:}
  expiration: ${JWT_EXPIRATION:604800000}

upload:
  path: ./uploads
  max-size: 10485760

export:
  path: ./exports
  retention-days: 7

feishu:
  app-id: ${FEISHU_APP_ID:}
  app-secret: ${FEISHU_APP_SECRET:}
  encryption-key: ${FEISHU_ENCRYPT_KEY:}
  verification-token: ${FEISHU_VERIFICATION_TOKEN:}
  redirect-uri: ${FEISHU_REDIRECT_URI:http://localhost:8080/attendance/api/feishu-auth/callback}
  bitable:
    app-token: ${BITABLE_APP_TOKEN:}
    table-id: ${BITABLE_TABLE_ID:}

attendance:
  upload:
    pdf-max-pages: 30
    pdf-render-dpi: 200
  prompt:
    # 内置标准模板版本；升级表头/字段后递增，启动时会刷新未自定义的国家提示词
    seed-version: 4
    seed-on-startup: true
    # 设为 true 时每次启动强制覆盖全部国家提示词（等同重新初始化）
    force-seed-on-startup: false
    import-markdown-when-empty: true

mimo:
  api-key: ${MIMO_API_KEY:}
  api-url: ${MIMO_API_URL:https://api.xiaomimimo.com/v1}
  # 识图须用 mimo-v2.5 或 mimo-v2-omni；mimo-v2.5-pro 不支持 image_url
  model: ${MIMO_MODEL:mimo-v2.5}
  temperature: ${MIMO_TEMPERATURE:0.3}
  max-tokens: ${MIMO_MAX_TOKENS:8192}
  top-p: ${MIMO_TOP_P:0.9}

logging:
  level:
    com.attendance: DEBUG
    org.springframework: INFO
  file:
    name: ./logs/attendance-assistant.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 10
      total-size-cap: 100MB
