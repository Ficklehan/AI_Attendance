/** 与 backend application.yml 中 server.servlet.context-path 保持一致 */
export const API_BASE_PATH = '/attendance/api'

/** Vite 开发代理目标（server.port） */
export const API_DEV_SERVER_ORIGIN = 'http://localhost:8080'
