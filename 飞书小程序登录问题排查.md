# 飞书小程序登录问题排查指南

## 0. 先确认运行模式

**域名、API 地址、本地/公网切换**统一见 **[docs/运维手册.md §5.2](docs/运维手册.md#52-域名与环境地址切换统一入口)**（唯一运维说明）。

| 模式 | 要点 |
|------|------|
| **本地开发者工具** | `production.yaml` → `runtime.mode: local`，执行 `./start.sh apply` |
| **手机飞书 / 公网** | `runtime.mode: public` + `./start.sh apply`（自动生成 `config.runtime.js` / `config.prod.js`） |

公网验收：`curl -s https://<PUBLIC_HOST>/attendance/api/feishu-auth/readiness`（期望 `feishuConfigured: true`）。

---

## 1. 检查开发者工具设置

在飞书开发者工具中：

1. 点击右上角「详情」
2. 选择「本地设置」
3. **本地调试**时勾选：
   - ✅ 不校验合法域名、web-view(业务域名)、TLS 版本以及 HTTPS 证书
4. **真机/公网**时取消「不校验合法域名」，并在飞书开放平台配置合法域名
5. 确保「项目配置」中的 appid 正确：`cli_a96dfac04b789cbb`

---

## 2. 查看控制台日志

点击「Console」，关注：

- `飞书小程序启动, API: ...`（启动时的 API 基址）
- 「点击登录按钮」
- 「获取授权码成功」
- 「请求登录接口，URL: ...」
- `[config] config.prod.js 不存在` → 需运行 render 脚本

---

## 3. 检查网络请求

点击「Network」，登录应对：

| 模式 | 期望请求 URL |
|------|----------------|
| 本地 | `http://localhost:8080/attendance/api/feishu-auth/miniprogram/login` |
| 公网 | `https://uat-guanpei.eminxing.com/attendance/api/feishu-auth/miniprogram/login` |

常见状态：

| 现象 | 可能原因 |
|------|----------|
| `request:fail network error` | 手机用了 localhost；或未 render / 未上传小程序 |
| HTTP 200 + 业务错误「飞书凭证未配置」 | 服务器缺 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` |
| HTTP 500 `errors.systemError` | 服务器无法访问 `open.feishu.cn`（DNS/网络） |
| HTTP 503 `errors.networkError` | 同上（网络类错误） |

---

## 4. 后端日志

```bash
# 本地
tail -f backend/logs/attendance-assistant.log

# 或 Spring Boot 控制台输出
```

关注：`飞书小程序登录`、`飞书凭证未配置`、`UnknownHostException: open.feishu.cn`

---

## 5. 常见问题

### 问题 1：手机出现 localhost / network error

- 手机**不能**访问 `localhost`
- `production.yaml` → `runtime.mode: public`
- 执行 `./start.sh apply`
- 重新上传小程序到飞书开发者工具

### 问题 2：授权码成功，登录接口失败

- 检查 API 基址是否与后端部署一致
- 公网：确认 Nginx 将 `/attendance/api` 转发到后端 8080
- 本地：确认 `./start.sh backend` 或 dev profile 已启动

### 问题 3：飞书凭证未配置

服务器环境变量（参考 `deploy/secrets.example`）：

```bash
FEISHU_APP_ID=cli_a96dfac04b789cbb
FEISHU_APP_SECRET=你的密钥
```

合并 rendered env 后重启后端：

```bash
set -a
source deploy/rendered/production.env
source /secure/path/secrets.env
set +a
```

### 问题 4：PC 飞书登录 500 / network error

PC OAuth 回调会请求 `open.feishu.cn` 换 token。本机需能解析该域名：

```bash
ping open.feishu.cn
curl -I https://open.feishu.cn
```

---

## 6. 临时调试（不改代码）

开发者工具 Console：

```javascript
tt.setStorageSync('apiBaseUrlOverride', 'https://uat-guanpei.eminxing.com/attendance/api')
```

清除覆盖：

```javascript
tt.removeStorageSync('apiBaseUrlOverride')
```

---

## 7. 相关文档

- [deploy/README.md](deploy/README.md) — 域名清单与服务器部署
- [docs/architecture-and-config.md](docs/architecture-and-config.md) — 架构与配置分层
